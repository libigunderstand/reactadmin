import Login from "_pages/Login"
import Home from "_pages/Home"

var routes = [
    {
        path: '/',
        component: Home
    },
    {
        path: '/login',
        component: Login
    }
]

export default routes